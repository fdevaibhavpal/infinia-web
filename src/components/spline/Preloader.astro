---
import PreloaderSpline from './PreloaderSpline.tsx'; // React based Preloader using Application
const { isLoaded } = Astro.props;
---

<div class={`fixed inset-0 bg-black flex items-center justify-center z-[9999] transition-opacity duration-700 ${isLoaded ? 'opacity-0 pointer-events-none' : 'opacity-100'}`}>
  <PreloaderSpline client:only="react" />
</div>
