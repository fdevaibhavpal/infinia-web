---
import LazySpline from './LazySplineLoader';
const { splineUrl, className, priority = false } = Astro.props;
---

<LazySpline client:only="react" splineUrl={splineUrl} className={className} priority={priority} />
